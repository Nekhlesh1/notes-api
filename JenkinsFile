// 
pipeline {
    agent any

    stages {
        stage('Docker Test') {
            steps {
                sh 'docker --version'
                sh 'docker run --rm node:16-alpine node --version'
            }
        }
    }
}